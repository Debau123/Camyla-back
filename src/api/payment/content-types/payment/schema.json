{
  "kind": "collectionType",
  "collectionName": "payments",
  "info": {
    "singularName": "payment",
    "pluralName": "payments",
    "displayName": "Payment"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "order": { "type": "relation", "relation": "oneToOne", "target": "api::order.order" },
    "status": { "type": "enumeration", "enum": ["pendiente", "completado", "fallido"] },
    "method": { "type": "enumeration", "enum": ["tarjeta", "paypal", "otro"] },
    "date": { "type": "date" },
    "transaction": { "type": "string" }
  }
}
